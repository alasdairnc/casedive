# CaseDive Weekly Digest — 2026-06-22

## Activity (last 7 days)
- 4 commits across 14 files — dominated by Stripe billing foundation
- `5e43d6c` feat(billing): Stripe freemium subscriptions + plan-aware rate limiting (#22) — B2C monetization phases 1–3: subscriptions table, plan-aware rate limiting, checkout/portal/webhook endpoints
- `bb463b1` fix(billing): merge checkout+portal into one function (12-fn cap) (#23) — Vercel Hobby plan allows 12 serverless functions; first deploy hit 13 and was rejected
- `fa3a4ad` fix(billing): correct logging arg order/args (prod 500 crash) (#24) — prod 500 immediately post-deploy; `logRequestStart(requestId, endpoint, req)` called in wrong arg order
- `065a540` chore(digest): weekly health digest 2026-06-15

## Open PRs
- #25 (draft, 1 day) — cross-platform dev setup + Node-based Claude Code hooks
- #21 (4 days) — retrieval autofix: loosen `relevance_min_token_overlap` 2→1 (needs review before merge)
- #20 (4 days) — fix(ci): unblock daily retrieval autofix PR (gitignored path caused `git add` to abort, so the loop has never actually opened a PR)
- #19 (5 days) — feat(caselaw): fix 24 fabricated pre-2000 SCC citations, add 16 verified cases, add family law
- #18 (9 days) — previous weekly digest (stale)
- #17 (35 days) — Dependabot: brace-expansion 5.0.5→5.0.6
- #16 (42 days) — digest 2026-05-11 (stale)
- #1, #2 (103 days) — Vercel bot draft PRs (stale)

## Merged this week
- #22 feat(billing): Stripe freemium subscriptions + plan-aware rate limiting — merged Jun 20
- #23 fix(billing): merge checkout+portal into one function — merged Jun 20
- #24 fix(billing): correct logging arg order (prod 500 crash) — merged Jun 20

## Hottest files
1. `tests/unit/billingApi.test.js` (3 touches) — billing test iteration
2. `api/billing.js` / `api/stripe-webhook.js` / `api/create-checkout-session.js` / `api/billing-portal.js` (2 each) — Stripe endpoint churn

## Retrieval health (from config)
- Token gate: `ai_citation_min_token_overlap: 2` (pre-verify) → `final_case_min_token_overlap: 3` (final selection). `relevance_min_token_overlap` is currently 2 — PR #21 proposes loosening to 1 based on 10 production no-case-law failures, but this is a broad change; the CI loop (#20) that auto-proposes these fixes has never successfully opened a PR until this week.

## Dependency drift
- `pdfkit` 0.18.0 → 0.19.1 (minor — worth a bump)
- `react` / `react-dom` 18 → 19 (major — skip)

## Watch list
1. **#19 (caselaw corpus)** — 5 days unmerged, fixes 24 fabricated SCC citations that may be live in production today. High-priority review.
2. **#20 before #21** — #20 fixes the CI bug that has silently prevented the retrieval-autofix loop from ever opening a real PR. Merge #20 first, then evaluate #21's threshold loosening with a full retrieval-regression run.
3. **Billing: Phase 4 (frontend) still absent** — checkout + portal backend is live but no UI; Stripe remains in test-mode. The `billing.js` combined endpoint is the integration point.
